{% extends '::base.front.html.twig' %}
{% block title %}
    {% if data.project %}
        {{ data.project.getTitle }}
    {% else %}
        404 - Page not found
    {% endif %}
{% endblock %}

{% block meta %}
     {% if data.project %}
        <meta name="keywords" content="{{data.project.getTitle}}"/>
        <meta name="description" content="{{data.project.getTitle}}"/>
    {% endif %}
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{asset('bundles/aliansstone/css/front.style.css')}}" type="text/css" media="screen"/> 
    <
{% endblock %}
{% block topjs %}
    {{parent()}}
    
{% endblock %}

{% block content %}
   <div class="wrap">
   {% if data.project %}
        <table>
        <tr>
        <td><h1>{{data.project.getTitle}}</h1></td>
        
        <td><img class="radius5" src="{{data.project.getRelativeLinkToThumbnailImg('150')}}"/></td>
        </tr>
        </table>
        
        <div class="material-sample">
        
        {% if data.images1 %}
        <h3>{{data.project.getText1}}</h3>
            <div class="gallery">
            {% for image in data.images1 %}
                <a href="{{image.getRelativeLinkToImg}}"><img class="radius5" src="{{image.getRelativeLinkToThumbnailImg('100')}}"/></a>
            {% endfor %}
            </div>
        {% endif %}
        </div>
        
        <div class="material-sample">
        
        {% if data.images2 %}
        <h3>{{data.project.getText2}}</h3>
            <div class="gallery">
            {% for image in data.images2 %}
                <a href="{{image.getRelativeLinkToImg}}"><img class="radius5" src="{{image.getRelativeLinkToThumbnailImg('100')}}"/></a>
            {% endfor %}
            </div>
        {% endif %}
        </div>
        <div class="material-sample">
        
        {% if data.images3 %}
        <h3>{{data.project.getText3}}</h3>
        <div class="gallery">
            {% for image in data.images3 %}
                <a href="{{image.getRelativeLinkToImg}}"><img class="radius5" src="{{image.getRelativeLinkToThumbnailImg('100')}}"/></a>
            {% endfor %}
            </div>
        {% endif %}
        </div>
        
        <div class="material-description">
        {{data.project.getContent | raw}}
        </div>
        
   {% else %}
        {% include 'AliansStoneBundle:Front:404.html.twig' %}
   {% endif %}
    </div>
{% endblock %}


{% block customjs %}
<script type="text/javascript">
    
    /******************/
    /* magnific-popup */
    /******************/
    $('.gallery').each(function() { // the containers for all your galleries
    $(this).magnificPopup({
        delegate: 'a', // the selector for gallery item
        type: 'image',
        gallery: {
          enabled:true
        }
    });
}); 
</script>
{% endblock %}
